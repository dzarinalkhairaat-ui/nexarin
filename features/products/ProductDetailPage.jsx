import ProductDetailScreen from "@/features/products/components/detail-page/ProductDetailScreen";

export default function ProductDetailPage({ slug }) {
  return <ProductDetailScreen slug={slug} />;
}
